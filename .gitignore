.env
.venv
.venv/
__pycache__/
tenants/
subscribers.enc
subscribers.json
contacts.enc
backups
backups/
*.pyc

# in-RAM conversations persisted across restarts (contains chat text)
sessions.pkl

# deploy target config (server IP, key path)
deploy/deploy.env

# engagement analytics (hashed tenant, no message content — still local-only)
analytics.db
analytics.db-*

# supervision store (encrypted coaching content — same class as sessions.pkl)
supervision.db
supervision.db-*

# harness delegate artefacts (ТЗ, логи прогонов)
.harness/

# macOS
.DS_Store
